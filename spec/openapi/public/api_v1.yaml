info:
  title: Identity Management API (Public)
  description: |
    ## Public API

    Authentication, user registration, session management and
    endpoints for the currently authenticated user.

    ### Authentication
    Use the **Access Tokens** endpoints to obtain a Bearer token via OAuth2.
    Include the token in all subsequent requests as `Authorization: Bearer <token>`.
  version: v1
  contact:
    name: Samedis-care GmbH
