# Version: 1.0
# auto generated path descriptions, update via: (append `-f` to force update if needed)
#     rails g openapi_spec v1_user_tenants
---
paths:
  "/api/v1/user/tenant":
    post:
      summary: 'Tenant: Create [POST] of the current user'
      tags:
      - Tenants
      operationId: user-tenants-create
      x-controller: Api::V1::User::TenantsController
      x-controller-action: create
      x-route-name: v1_user_tenants
      x-candos:
      - public
      x-model-type: Actors::Tenant
      x-record-type: actor
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - public
      security:
      - Bearer: []
      parameters:
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::Tenant created
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: actor
                      attributes:
                        "$ref": "#/components/schemas/ActorSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true
          application/x-www-form-urlencoded:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true
          multipart/form-data:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true
    get:
      summary: 'Tenants: List [GET] of the current user'
      tags:
      - Tenants
      operationId: user-tenants-index
      x-controller: Api::V1::User::TenantsController
      x-controller-action: index
      x-route-name: v1_user_tenants
      x-candos:
      - public
      x-model-type: Actors::Tenant
      x-record-type: actor
      x-gridfilter-fields:
      - _id
      - _keywords
      - _type
      - active
      - actor_settings
      - auto
      - children_count
      - created_at
      - defaults_checksum
      - deleted
      - deleted_at
      - depth
      - full_name
      - image_data
      - map_actor_id
      - modules_selected
      - name
      - parent_id
      - parent_ids
      - path
      - path_ids
      - role_ids
      - short_name
      - system
      - template_actor_id
      - title
      - updated_at
      - write_protected
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - public
      security:
      - Bearer: []
      parameters:
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      - name: quickfilter
        in: query
        schema:
          type: string
        description: performs a search in keywords
      - name: page[number]
        in: query
        schema:
          type: string
        x-example: '1'
        description: |
          the page number (starts at 1, if not given or lower than 1 then
          page 1 will automatically be assumed)'
      - name: page[limit]
        in: query
        schema:
          type: string
        x-example: '10'
        description: the number of records per page (might be limited on the server
          side, use 0 to fetch no data to get only the total count)
      - name: padding
        in: query
        schema:
          type: string
        x-example: '0'
        description: 'manually adjust db offset: (page*per_page)+padding not required
          for normal paging, in doubt omit'
      - name: sort
        in: query
        schema:
          type: string
        description: |
          JSON Array of Objects to sort the results by one
          or more fields in the requested order.
          Example:
          `[{ property: "field1", direction: "ASC" }, { property: "field2", direction: "DESC" }]`
      - name: gridfilter
        in: query
        schema:
          type: string
        description: |
          String in JSON format for grid-based filtering.

          <details>
            <summary>The model defined these fields as allowed for filtering.</summary>

            `_id`, `_keywords`, `_type`, `active`, `actor_settings`, `auto`, `children_count`, `created_at`, `defaults_checksum`, `deleted`, `deleted_at`, `depth`, `full_name`, `image_data`, `map_actor_id`, `modules_selected`, `name`, `parent_id`, `parent_ids`, `path`, `path_ids`, `role_ids`, `short_name`, `system`, `template_actor_id`, `title`, `updated_at`, `write_protected`

          </details>
      responses:
        '200':
          description: Actors::Tenants list
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: List endpoints will return an array below data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - type
                      - attributes
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          default: actor
                        attributes:
                          "$ref": "#/components/schemas/ActorOverviewSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
  "/api/v1/user/tenant/{id}":
    delete:
      summary: 'Tenant: Delete [DELETE] of the current user'
      tags:
      - Tenants
      operationId: user-tenants-destroy
      x-controller: Api::V1::User::TenantsController
      x-controller-action: destroy
      x-route-name: v1_user_tenants
      x-candos:
      - identity-management/actors.writer
      - "~/app-tenant.admin"
      - "~/tenant.admin"
      x-model-type: Actors::Tenant
      x-record-type: actor
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - identity-management/actors.writer
         - ~/app-tenant.admin
         - ~/tenant.admin
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: id
        in: path
        required: true
        description: Tenant id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::Tenant deleted
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/meta"
        '409':
          description: Conflict
          content:
            application/json:
              schema:
                type: object
                description: |-
                  Some server side rule prevented finishing this request.
                  For example a multi delete might not have worked on all
                  requested records due to some rule not allowing the delete.
                  Please refer to the details in the response like
                  - `meta.msg.error` error identifier
                  - `meta.msg.message` message being displayed
                  - `meta.msg.error_details` showing the attribute causing the problem
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
    get:
      summary: 'Tenant: Show [GET] of the current user'
      tags:
      - Tenants
      operationId: user-tenants-show
      x-controller: Api::V1::User::TenantsController
      x-controller-action: show
      x-route-name: v1_user_tenants
      x-candos:
      - public
      x-model-type: Actors::Tenant
      x-record-type: actor
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - public
      security:
      - Bearer: []
      parameters:
      - name: id
        in: path
        required: true
        description: Tenant id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::Tenant view
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: actor
                      attributes:
                        "$ref": "#/components/schemas/ActorSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
    put:
      summary: 'Tenant: Update [PUT] of the current user'
      tags:
      - Tenants
      operationId: user-tenants-update
      x-controller: Api::V1::User::TenantsController
      x-controller-action: update
      x-route-name: v1_user_tenants
      x-candos:
      - identity-management/actors.writer
      - "~/app-tenant.admin"
      - "~/tenant.admin"
      x-model-type: Actors::Tenant
      x-record-type: actor
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - identity-management/actors.writer
         - ~/app-tenant.admin
         - ~/tenant.admin
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: id
        in: path
        required: true
        description: Tenant id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::Tenant updated
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: actor
                      attributes:
                        "$ref": "#/components/schemas/ActorSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true
          application/x-www-form-urlencoded:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true
          multipart/form-data:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true

