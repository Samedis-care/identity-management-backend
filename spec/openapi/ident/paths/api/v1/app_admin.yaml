# Version: 1.0
# auto generated path descriptions, update via: (append `-f` to force update if needed)
#     rails g openapi_spec v1_app_admin_index
---
paths:
  "/api/v1/app_admin":
    post:
      summary: 'App: Create [POST]'
      tags:
      - Apps
      operationId: appadmin-create
      x-controller: Api::V1::AppAdminController
      x-controller-action: create
      x-route-name: v1_app_admin_index
      x-candos:
      - identity-management/apps.writer
      x-model-type: Actors::App
      x-record-type: app_admin
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - identity-management/apps.writer
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::App created
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: app_admin
                      attributes:
                        "$ref": "#/components/schemas/AppAdminSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: object
                  description: the main attributes of this record
                  properties:
                    config:
                      type: object
                      description: Hash of app config
                      properties:
                        url:
                          type: string
                          default: https://domain.local
                          description: URL of the app
                        uses_bearer_token:
                          type: boolean
                          default: true
                          description: 'If the app bearer token will be included in
                            redirect URL as #anchor (true) or query param for server
                            (false)'
                        locales:
                          type: array
                          default:
                          - de
                          - en
                          items:
                            type: string
                            description: locale string
                        default_locale:
                          type: string
                          default: en
                          description: Which of the locales is the default
                        mailer:
                          type: object
                          description: mail config
                          properties:
                            from:
                              type: string
                              description: The from address used in mails for user
                                email confirmations
                            reply_to:
                              type: string
                              description: The reply address used in mails for user
                                email confirmations
                            support_email:
                              type: string
                              description: The support address used in mails for user
                                email confirmations
                            logo_b64:
                              type: string
                              description: A PNG logo as mail header in Base64 encoded
                                format
                            footer_html:
                              type: string
                              description: The footer_html of the current client locale
                            footer_html_translations:
                              type: object
                              description: language keys here are dynamic and can
                                consist of any supported language, minimum should
                                be those languages of the configured app locales
                              properties:
                                en:
                                  type: string
                                  description: the english locale footer
                                de:
                                  type: string
                                  description: the german locale footer
                            smtp_settings:
                              type: object
                              description: smtp settings
                              properties:
                                address:
                                  type: string
                                  description: Address of SMTP server
                                port:
                                  type: number
                                  description: Port of SMTP server
                                domain:
                                  type: string
                                  description: HELO domain
                                user_name:
                                  type: string
                                  description: SMTP username
                                password:
                                  type: string
                                  description: SMTP password
                                authentication:
                                  type: string
                                  description: 'AuthenticationMode: plain, login or
                                    cram_md5'
                                enable_starttls:
                                  type: boolean
                                  description: Use STARTTLS when connecting to your
                                    SMTP server and fail if unsupported
                                enable_starttls_auto:
                                  type: boolean
                                  description: Detects if STARTTLS is enabled in your
                                    SMTP server and starts to use it
                                openssl_verify_mode:
                                  type: string
                                  description: When using TLS, you can set how OpenSSL
                                    checks the certificate. This is really useful
                                    if you need to validate a self-signed and/or a
                                    wildcard certificate. You can use the name of
                                    an OpenSSL verify constant ('none' or 'peer')
                                ssl:
                                  type: string
                                  description: 'Enables the SMTP connection to use
                                    SMTP/TLS (SMTPS: SMTP over direct TLS connection)'
                                open_timeout:
                                  type: number
                                  description: Number of seconds to wait while attempting
                                    to open a connection.
                                read_timeout:
                                  type: number
                                  description: Number of seconds to wait until timing-out
                                    a read(2) call.
                        theme:
                          type: object
                          description: optional theme settings
                          properties:
                            primary:
                              type: object
                              description: primary colors
                              properties:
                                main:
                                  type: string
                                  description: primary color
                                light:
                                  type: string
                                  description: lighter variant of the primary color
                                dark:
                                  type: string
                                  description: darker variant of the primary color
                            secondary:
                              type: object
                              description: secondary colors
                              properties:
                                main:
                                  type: string
                                  description: secondary color
                                light:
                                  type: string
                                  description: lighter variant of the secondary color
                                dark:
                                  type: string
                                  description: darker variant of the secondary color
                            background:
                              type: object
                              description: background
                              properties:
                                default:
                                  type: string
                                  description: background color
                                advanced:
                                  type: string
                                  description: background CSS property
                                  nullable: true
                            components_care:
                              type: object
                              description: components care specific settings
                              properties:
                                ui_kit:
                                  type: object
                                  description: theme settings for ui_kit
                                  properties:
                                    action_button:
                                      type: object
                                      description: theme settings for ActionButton
                                      properties:
                                        background_color:
                                          type: string
                                          description: background color
                            type:
                              type: string
                              default: light
                              description: light or dark theme as base
                    full_name:
                      type: string
                      default: app-name
                      description: full name of the app
                    import_candos:
                      type: string
                      description: yaml data with array of all candos the app has
                    import_roles:
                      type: string
                      description: yaml data with array of all roles the app has and
                        assigned candos
                    locale_import_candos:
                      type: object
                      description: language keys here are dynamic and can consist
                        of any supported language, minimum should be those languages
                        of the configured app locales
                      properties:
                        en:
                          type: string
                          description: the english locales for this apps functionalities
                            as yaml string
                        de:
                          type: string
                          description: the german locales for this apps functionalities
                            as yaml string
                    locale_import_roles:
                      type: object
                      description: language keys here are dynamic and can consist
                        of any supported language, minimum should be those languages
                        of the configured app locales
                      properties:
                        en:
                          type: string
                          description: the english locales for this apps roles as
                            yaml string
                        de:
                          type: string
                          description: the german locales for this apps roles as yaml
                            string
                    short_name:
                      type: string
                      default: app-name
                      description: short name of the app
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                data[config][default_locale]:
                  type: string
                  default: en
                  description: Which of the locales is the default
                data[config][locales]:
                  type: array
                  default:
                  - de
                  - en
                  items:
                    type: string
                    description: locale string
                data[config][mailer][footer_html]:
                  type: string
                  description: The footer_html of the current client locale
                data[config][mailer][footer_html_translations][de]:
                  type: string
                  description: the german locale footer
                data[config][mailer][footer_html_translations][en]:
                  type: string
                  description: the english locale footer
                data[config][mailer][from]:
                  type: string
                  description: The from address used in mails for user email confirmations
                data[config][mailer][logo_b64]:
                  type: string
                  description: A PNG logo as mail header in Base64 encoded format
                data[config][mailer][reply_to]:
                  type: string
                  description: The reply address used in mails for user email confirmations
                data[config][mailer][smtp_settings][address]:
                  type: string
                  description: Address of SMTP server
                data[config][mailer][smtp_settings][authentication]:
                  type: string
                  description: 'AuthenticationMode: plain, login or cram_md5'
                data[config][mailer][smtp_settings][domain]:
                  type: string
                  description: HELO domain
                data[config][mailer][smtp_settings][enable_starttls]:
                  type: boolean
                  description: Use STARTTLS when connecting to your SMTP server and
                    fail if unsupported
                data[config][mailer][smtp_settings][enable_starttls_auto]:
                  type: boolean
                  description: Detects if STARTTLS is enabled in your SMTP server
                    and starts to use it
                data[config][mailer][smtp_settings][open_timeout]:
                  type: number
                  description: Number of seconds to wait while attempting to open
                    a connection.
                data[config][mailer][smtp_settings][openssl_verify_mode]:
                  type: string
                  description: When using TLS, you can set how OpenSSL checks the
                    certificate. This is really useful if you need to validate a self-signed
                    and/or a wildcard certificate. You can use the name of an OpenSSL
                    verify constant ('none' or 'peer')
                data[config][mailer][smtp_settings][password]:
                  type: string
                  description: SMTP password
                data[config][mailer][smtp_settings][port]:
                  type: number
                  description: Port of SMTP server
                data[config][mailer][smtp_settings][read_timeout]:
                  type: number
                  description: Number of seconds to wait until timing-out a read(2)
                    call.
                data[config][mailer][smtp_settings][ssl]:
                  type: string
                  description: 'Enables the SMTP connection to use SMTP/TLS (SMTPS:
                    SMTP over direct TLS connection)'
                data[config][mailer][smtp_settings][user_name]:
                  type: string
                  description: SMTP username
                data[config][mailer][support_email]:
                  type: string
                  description: The support address used in mails for user email confirmations
                data[config][theme][background][advanced]:
                  type: string
                  description: background CSS property
                  nullable: true
                data[config][theme][background][default]:
                  type: string
                  description: background color
                data[config][theme][components_care][ui_kit][action_button][background_color]:
                  type: string
                  description: background color
                data[config][theme][primary][dark]:
                  type: string
                  description: darker variant of the primary color
                data[config][theme][primary][light]:
                  type: string
                  description: lighter variant of the primary color
                data[config][theme][primary][main]:
                  type: string
                  description: primary color
                data[config][theme][secondary][dark]:
                  type: string
                  description: darker variant of the secondary color
                data[config][theme][secondary][light]:
                  type: string
                  description: lighter variant of the secondary color
                data[config][theme][secondary][main]:
                  type: string
                  description: secondary color
                data[config][theme][type]:
                  type: string
                  default: light
                  description: light or dark theme as base
                data[config][url]:
                  type: string
                  default: https://domain.local
                  description: URL of the app
                data[config][uses_bearer_token]:
                  type: boolean
                  default: true
                  description: 'If the app bearer token will be included in redirect
                    URL as #anchor (true) or query param for server (false)'
                data[full_name]:
                  type: string
                  default: app-name
                  description: full name of the app
                data[import_candos]:
                  type: string
                  description: yaml data with array of all candos the app has
                data[import_roles]:
                  type: string
                  description: yaml data with array of all roles the app has and assigned
                    candos
                data[locale_import_candos][de]:
                  type: string
                  description: the german locales for this apps functionalities as
                    yaml string
                data[locale_import_candos][en]:
                  type: string
                  description: the english locales for this apps functionalities as
                    yaml string
                data[locale_import_roles][de]:
                  type: string
                  description: the german locales for this apps roles as yaml string
                data[locale_import_roles][en]:
                  type: string
                  description: the english locales for this apps roles as yaml string
                data[short_name]:
                  type: string
                  default: app-name
                  description: short name of the app
          multipart/form-data:
            schema:
              type: object
              properties:
                data[config][default_locale]:
                  type: string
                  default: en
                  description: Which of the locales is the default
                data[config][locales]:
                  type: array
                  default:
                  - de
                  - en
                  items:
                    type: string
                    description: locale string
                data[config][mailer][footer_html]:
                  type: string
                  description: The footer_html of the current client locale
                data[config][mailer][footer_html_translations][de]:
                  type: string
                  description: the german locale footer
                data[config][mailer][footer_html_translations][en]:
                  type: string
                  description: the english locale footer
                data[config][mailer][from]:
                  type: string
                  description: The from address used in mails for user email confirmations
                data[config][mailer][logo_b64]:
                  type: string
                  description: A PNG logo as mail header in Base64 encoded format
                data[config][mailer][reply_to]:
                  type: string
                  description: The reply address used in mails for user email confirmations
                data[config][mailer][smtp_settings][address]:
                  type: string
                  description: Address of SMTP server
                data[config][mailer][smtp_settings][authentication]:
                  type: string
                  description: 'AuthenticationMode: plain, login or cram_md5'
                data[config][mailer][smtp_settings][domain]:
                  type: string
                  description: HELO domain
                data[config][mailer][smtp_settings][enable_starttls]:
                  type: boolean
                  description: Use STARTTLS when connecting to your SMTP server and
                    fail if unsupported
                data[config][mailer][smtp_settings][enable_starttls_auto]:
                  type: boolean
                  description: Detects if STARTTLS is enabled in your SMTP server
                    and starts to use it
                data[config][mailer][smtp_settings][open_timeout]:
                  type: number
                  description: Number of seconds to wait while attempting to open
                    a connection.
                data[config][mailer][smtp_settings][openssl_verify_mode]:
                  type: string
                  description: When using TLS, you can set how OpenSSL checks the
                    certificate. This is really useful if you need to validate a self-signed
                    and/or a wildcard certificate. You can use the name of an OpenSSL
                    verify constant ('none' or 'peer')
                data[config][mailer][smtp_settings][password]:
                  type: string
                  description: SMTP password
                data[config][mailer][smtp_settings][port]:
                  type: number
                  description: Port of SMTP server
                data[config][mailer][smtp_settings][read_timeout]:
                  type: number
                  description: Number of seconds to wait until timing-out a read(2)
                    call.
                data[config][mailer][smtp_settings][ssl]:
                  type: string
                  description: 'Enables the SMTP connection to use SMTP/TLS (SMTPS:
                    SMTP over direct TLS connection)'
                data[config][mailer][smtp_settings][user_name]:
                  type: string
                  description: SMTP username
                data[config][mailer][support_email]:
                  type: string
                  description: The support address used in mails for user email confirmations
                data[config][theme][background][advanced]:
                  type: string
                  description: background CSS property
                  nullable: true
                data[config][theme][background][default]:
                  type: string
                  description: background color
                data[config][theme][components_care][ui_kit][action_button][background_color]:
                  type: string
                  description: background color
                data[config][theme][primary][dark]:
                  type: string
                  description: darker variant of the primary color
                data[config][theme][primary][light]:
                  type: string
                  description: lighter variant of the primary color
                data[config][theme][primary][main]:
                  type: string
                  description: primary color
                data[config][theme][secondary][dark]:
                  type: string
                  description: darker variant of the secondary color
                data[config][theme][secondary][light]:
                  type: string
                  description: lighter variant of the secondary color
                data[config][theme][secondary][main]:
                  type: string
                  description: secondary color
                data[config][theme][type]:
                  type: string
                  default: light
                  description: light or dark theme as base
                data[config][url]:
                  type: string
                  default: https://domain.local
                  description: URL of the app
                data[config][uses_bearer_token]:
                  type: boolean
                  default: true
                  description: 'If the app bearer token will be included in redirect
                    URL as #anchor (true) or query param for server (false)'
                data[full_name]:
                  type: string
                  default: app-name
                  description: full name of the app
                data[import_candos]:
                  type: string
                  description: yaml data with array of all candos the app has
                data[import_roles]:
                  type: string
                  description: yaml data with array of all roles the app has and assigned
                    candos
                data[locale_import_candos][de]:
                  type: string
                  description: the german locales for this apps functionalities as
                    yaml string
                data[locale_import_candos][en]:
                  type: string
                  description: the english locales for this apps functionalities as
                    yaml string
                data[locale_import_roles][de]:
                  type: string
                  description: the german locales for this apps roles as yaml string
                data[locale_import_roles][en]:
                  type: string
                  description: the english locales for this apps roles as yaml string
                data[short_name]:
                  type: string
                  default: app-name
                  description: short name of the app
    get:
      summary: 'Apps: List [GET]'
      tags:
      - Apps
      operationId: appadmin-index
      x-controller: Api::V1::AppAdminController
      x-controller-action: index
      x-route-name: v1_app_admin_index
      x-candos:
      - identity-management/apps.reader
      x-model-type: Actors::App
      x-record-type: app_admin
      x-gridfilter-fields:
      - _id
      - _keywords
      - _type
      - active
      - actor_settings
      - auto
      - children_count
      - created_at
      - defaults_checksum
      - deleted
      - deleted_at
      - depth
      - full_name
      - image_data
      - map_actor_id
      - name
      - parent_id
      - parent_ids
      - path
      - path_ids
      - role_ids
      - short_name
      - system
      - template_actor_id
      - title
      - updated_at
      - write_protected
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - identity-management/apps.reader
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      - name: quickfilter
        in: query
        schema:
          type: string
        description: performs a search in keywords
      - name: page[number]
        in: query
        schema:
          type: string
        x-example: '1'
        description: |
          the page number (starts at 1, if not given or lower than 1 then
          page 1 will automatically be assumed)'
      - name: page[limit]
        in: query
        schema:
          type: string
        x-example: '10'
        description: the number of records per page (might be limited on the server
          side, use 0 to fetch no data to get only the total count)
      - name: padding
        in: query
        schema:
          type: string
        x-example: '0'
        description: 'manually adjust db offset: (page*per_page)+padding not required
          for normal paging, in doubt omit'
      - name: sort
        in: query
        schema:
          type: string
        description: |
          JSON Array of Objects to sort the results by one
          or more fields in the requested order.
          Example:
          `[{ property: "field1", direction: "ASC" }, { property: "field2", direction: "DESC" }]`
      - name: gridfilter
        in: query
        schema:
          type: string
        description: |
          String in JSON format for grid-based filtering.

          <details>
            <summary>The model defined these fields as allowed for filtering.</summary>

            `_id`, `_keywords`, `_type`, `active`, `actor_settings`, `auto`, `children_count`, `created_at`, `defaults_checksum`, `deleted`, `deleted_at`, `depth`, `full_name`, `image_data`, `map_actor_id`, `name`, `parent_id`, `parent_ids`, `path`, `path_ids`, `role_ids`, `short_name`, `system`, `template_actor_id`, `title`, `updated_at`, `write_protected`

          </details>
      responses:
        '200':
          description: Actors::Apps list
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: List endpoints will return an array below data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - type
                      - attributes
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          default: app_admin_overview
                        attributes:
                          "$ref": "#/components/schemas/AppAdminOverviewSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
  "/api/v1/app_admin/{id}":
    delete:
      summary: 'App: Delete [DELETE]'
      tags:
      - Apps
      operationId: appadmin-destroy
      x-controller: Api::V1::AppAdminController
      x-controller-action: destroy
      x-route-name: v1_app_admin_index
      x-candos:
      - identity-management/apps.deleter
      x-model-type: Actors::App
      x-record-type: app_admin
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - identity-management/apps.deleter
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: id
        in: path
        required: true
        description: App id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::App deleted
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/meta"
        '409':
          description: Conflict
          content:
            application/json:
              schema:
                type: object
                description: |-
                  Some server side rule prevented finishing this request.
                  For example a multi delete might not have worked on all
                  requested records due to some rule not allowing the delete.
                  Please refer to the details in the response like
                  - `meta.msg.error` error identifier
                  - `meta.msg.message` message being displayed
                  - `meta.msg.error_details` showing the attribute causing the problem
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
    get:
      summary: 'App: Show [GET]'
      tags:
      - Apps
      operationId: appadmin-show
      x-controller: Api::V1::AppAdminController
      x-controller-action: show
      x-route-name: v1_app_admin_index
      x-candos:
      - identity-management/apps.reader
      x-model-type: Actors::App
      x-record-type: app_admin
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - identity-management/apps.reader
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: id
        in: path
        required: true
        description: App id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::App view
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: app_admin
                      attributes:
                        "$ref": "#/components/schemas/AppAdminSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
    put:
      summary: 'App: Update [PUT]'
      tags:
      - Apps
      operationId: appadmin-update
      x-controller: Api::V1::AppAdminController
      x-controller-action: update
      x-route-name: v1_app_admin_index
      x-candos:
      - identity-management/apps.writer
      x-model-type: Actors::App
      x-record-type: app_admin
      description: |-
        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - identity-management/apps.writer
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: id
        in: path
        required: true
        description: App id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::App updated
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: app_admin
                      attributes:
                        "$ref": "#/components/schemas/AppAdminSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: object
                  description: the main attributes of this record
                  properties:
                    config:
                      type: object
                      description: Hash of app config
                      properties:
                        url:
                          type: string
                          default: https://domain.local
                          description: URL of the app
                        uses_bearer_token:
                          type: boolean
                          default: true
                          description: 'If the app bearer token will be included in
                            redirect URL as #anchor (true) or query param for server
                            (false)'
                        locales:
                          type: array
                          default:
                          - de
                          - en
                          items:
                            type: string
                            description: locale string
                        default_locale:
                          type: string
                          default: en
                          description: Which of the locales is the default
                        mailer:
                          type: object
                          description: mail config
                          properties:
                            from:
                              type: string
                              description: The from address used in mails for user
                                email confirmations
                            reply_to:
                              type: string
                              description: The reply address used in mails for user
                                email confirmations
                            support_email:
                              type: string
                              description: The support address used in mails for user
                                email confirmations
                            logo_b64:
                              type: string
                              description: A PNG logo as mail header in Base64 encoded
                                format
                            footer_html:
                              type: string
                              description: The footer_html of the current client locale
                            footer_html_translations:
                              type: object
                              description: language keys here are dynamic and can
                                consist of any supported language, minimum should
                                be those languages of the configured app locales
                              properties:
                                en:
                                  type: string
                                  description: the english locale footer
                                de:
                                  type: string
                                  description: the german locale footer
                            smtp_settings:
                              type: object
                              description: smtp settings
                              properties:
                                address:
                                  type: string
                                  description: Address of SMTP server
                                port:
                                  type: number
                                  description: Port of SMTP server
                                domain:
                                  type: string
                                  description: HELO domain
                                user_name:
                                  type: string
                                  description: SMTP username
                                password:
                                  type: string
                                  description: SMTP password
                                authentication:
                                  type: string
                                  description: 'AuthenticationMode: plain, login or
                                    cram_md5'
                                enable_starttls:
                                  type: boolean
                                  description: Use STARTTLS when connecting to your
                                    SMTP server and fail if unsupported
                                enable_starttls_auto:
                                  type: boolean
                                  description: Detects if STARTTLS is enabled in your
                                    SMTP server and starts to use it
                                openssl_verify_mode:
                                  type: string
                                  description: When using TLS, you can set how OpenSSL
                                    checks the certificate. This is really useful
                                    if you need to validate a self-signed and/or a
                                    wildcard certificate. You can use the name of
                                    an OpenSSL verify constant ('none' or 'peer')
                                ssl:
                                  type: string
                                  description: 'Enables the SMTP connection to use
                                    SMTP/TLS (SMTPS: SMTP over direct TLS connection)'
                                open_timeout:
                                  type: number
                                  description: Number of seconds to wait while attempting
                                    to open a connection.
                                read_timeout:
                                  type: number
                                  description: Number of seconds to wait until timing-out
                                    a read(2) call.
                        theme:
                          type: object
                          description: optional theme settings
                          properties:
                            primary:
                              type: object
                              description: primary colors
                              properties:
                                main:
                                  type: string
                                  description: primary color
                                light:
                                  type: string
                                  description: lighter variant of the primary color
                                dark:
                                  type: string
                                  description: darker variant of the primary color
                            secondary:
                              type: object
                              description: secondary colors
                              properties:
                                main:
                                  type: string
                                  description: secondary color
                                light:
                                  type: string
                                  description: lighter variant of the secondary color
                                dark:
                                  type: string
                                  description: darker variant of the secondary color
                            background:
                              type: object
                              description: background
                              properties:
                                default:
                                  type: string
                                  description: background color
                                advanced:
                                  type: string
                                  description: background CSS property
                                  nullable: true
                            components_care:
                              type: object
                              description: components care specific settings
                              properties:
                                ui_kit:
                                  type: object
                                  description: theme settings for ui_kit
                                  properties:
                                    action_button:
                                      type: object
                                      description: theme settings for ActionButton
                                      properties:
                                        background_color:
                                          type: string
                                          description: background color
                            type:
                              type: string
                              default: light
                              description: light or dark theme as base
                    full_name:
                      type: string
                      default: app-name
                      description: full name of the app
                    import_candos:
                      type: string
                      description: yaml data with array of all candos the app has
                    import_roles:
                      type: string
                      description: yaml data with array of all roles the app has and
                        assigned candos
                    locale_import_candos:
                      type: object
                      description: language keys here are dynamic and can consist
                        of any supported language, minimum should be those languages
                        of the configured app locales
                      properties:
                        en:
                          type: string
                          description: the english locales for this apps functionalities
                            as yaml string
                        de:
                          type: string
                          description: the german locales for this apps functionalities
                            as yaml string
                    locale_import_roles:
                      type: object
                      description: language keys here are dynamic and can consist
                        of any supported language, minimum should be those languages
                        of the configured app locales
                      properties:
                        en:
                          type: string
                          description: the english locales for this apps roles as
                            yaml string
                        de:
                          type: string
                          description: the german locales for this apps roles as yaml
                            string
                    short_name:
                      type: string
                      default: app-name
                      description: short name of the app
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                data[config][default_locale]:
                  type: string
                  default: en
                  description: Which of the locales is the default
                data[config][locales]:
                  type: array
                  default:
                  - de
                  - en
                  items:
                    type: string
                    description: locale string
                data[config][mailer][footer_html]:
                  type: string
                  description: The footer_html of the current client locale
                data[config][mailer][footer_html_translations][de]:
                  type: string
                  description: the german locale footer
                data[config][mailer][footer_html_translations][en]:
                  type: string
                  description: the english locale footer
                data[config][mailer][from]:
                  type: string
                  description: The from address used in mails for user email confirmations
                data[config][mailer][logo_b64]:
                  type: string
                  description: A PNG logo as mail header in Base64 encoded format
                data[config][mailer][reply_to]:
                  type: string
                  description: The reply address used in mails for user email confirmations
                data[config][mailer][smtp_settings][address]:
                  type: string
                  description: Address of SMTP server
                data[config][mailer][smtp_settings][authentication]:
                  type: string
                  description: 'AuthenticationMode: plain, login or cram_md5'
                data[config][mailer][smtp_settings][domain]:
                  type: string
                  description: HELO domain
                data[config][mailer][smtp_settings][enable_starttls]:
                  type: boolean
                  description: Use STARTTLS when connecting to your SMTP server and
                    fail if unsupported
                data[config][mailer][smtp_settings][enable_starttls_auto]:
                  type: boolean
                  description: Detects if STARTTLS is enabled in your SMTP server
                    and starts to use it
                data[config][mailer][smtp_settings][open_timeout]:
                  type: number
                  description: Number of seconds to wait while attempting to open
                    a connection.
                data[config][mailer][smtp_settings][openssl_verify_mode]:
                  type: string
                  description: When using TLS, you can set how OpenSSL checks the
                    certificate. This is really useful if you need to validate a self-signed
                    and/or a wildcard certificate. You can use the name of an OpenSSL
                    verify constant ('none' or 'peer')
                data[config][mailer][smtp_settings][password]:
                  type: string
                  description: SMTP password
                data[config][mailer][smtp_settings][port]:
                  type: number
                  description: Port of SMTP server
                data[config][mailer][smtp_settings][read_timeout]:
                  type: number
                  description: Number of seconds to wait until timing-out a read(2)
                    call.
                data[config][mailer][smtp_settings][ssl]:
                  type: string
                  description: 'Enables the SMTP connection to use SMTP/TLS (SMTPS:
                    SMTP over direct TLS connection)'
                data[config][mailer][smtp_settings][user_name]:
                  type: string
                  description: SMTP username
                data[config][mailer][support_email]:
                  type: string
                  description: The support address used in mails for user email confirmations
                data[config][theme][background][advanced]:
                  type: string
                  description: background CSS property
                  nullable: true
                data[config][theme][background][default]:
                  type: string
                  description: background color
                data[config][theme][components_care][ui_kit][action_button][background_color]:
                  type: string
                  description: background color
                data[config][theme][primary][dark]:
                  type: string
                  description: darker variant of the primary color
                data[config][theme][primary][light]:
                  type: string
                  description: lighter variant of the primary color
                data[config][theme][primary][main]:
                  type: string
                  description: primary color
                data[config][theme][secondary][dark]:
                  type: string
                  description: darker variant of the secondary color
                data[config][theme][secondary][light]:
                  type: string
                  description: lighter variant of the secondary color
                data[config][theme][secondary][main]:
                  type: string
                  description: secondary color
                data[config][theme][type]:
                  type: string
                  default: light
                  description: light or dark theme as base
                data[config][url]:
                  type: string
                  default: https://domain.local
                  description: URL of the app
                data[config][uses_bearer_token]:
                  type: boolean
                  default: true
                  description: 'If the app bearer token will be included in redirect
                    URL as #anchor (true) or query param for server (false)'
                data[full_name]:
                  type: string
                  default: app-name
                  description: full name of the app
                data[import_candos]:
                  type: string
                  description: yaml data with array of all candos the app has
                data[import_roles]:
                  type: string
                  description: yaml data with array of all roles the app has and assigned
                    candos
                data[locale_import_candos][de]:
                  type: string
                  description: the german locales for this apps functionalities as
                    yaml string
                data[locale_import_candos][en]:
                  type: string
                  description: the english locales for this apps functionalities as
                    yaml string
                data[locale_import_roles][de]:
                  type: string
                  description: the german locales for this apps roles as yaml string
                data[locale_import_roles][en]:
                  type: string
                  description: the english locales for this apps roles as yaml string
                data[short_name]:
                  type: string
                  default: app-name
                  description: short name of the app
          multipart/form-data:
            schema:
              type: object
              properties:
                data[config][default_locale]:
                  type: string
                  default: en
                  description: Which of the locales is the default
                data[config][locales]:
                  type: array
                  default:
                  - de
                  - en
                  items:
                    type: string
                    description: locale string
                data[config][mailer][footer_html]:
                  type: string
                  description: The footer_html of the current client locale
                data[config][mailer][footer_html_translations][de]:
                  type: string
                  description: the german locale footer
                data[config][mailer][footer_html_translations][en]:
                  type: string
                  description: the english locale footer
                data[config][mailer][from]:
                  type: string
                  description: The from address used in mails for user email confirmations
                data[config][mailer][logo_b64]:
                  type: string
                  description: A PNG logo as mail header in Base64 encoded format
                data[config][mailer][reply_to]:
                  type: string
                  description: The reply address used in mails for user email confirmations
                data[config][mailer][smtp_settings][address]:
                  type: string
                  description: Address of SMTP server
                data[config][mailer][smtp_settings][authentication]:
                  type: string
                  description: 'AuthenticationMode: plain, login or cram_md5'
                data[config][mailer][smtp_settings][domain]:
                  type: string
                  description: HELO domain
                data[config][mailer][smtp_settings][enable_starttls]:
                  type: boolean
                  description: Use STARTTLS when connecting to your SMTP server and
                    fail if unsupported
                data[config][mailer][smtp_settings][enable_starttls_auto]:
                  type: boolean
                  description: Detects if STARTTLS is enabled in your SMTP server
                    and starts to use it
                data[config][mailer][smtp_settings][open_timeout]:
                  type: number
                  description: Number of seconds to wait while attempting to open
                    a connection.
                data[config][mailer][smtp_settings][openssl_verify_mode]:
                  type: string
                  description: When using TLS, you can set how OpenSSL checks the
                    certificate. This is really useful if you need to validate a self-signed
                    and/or a wildcard certificate. You can use the name of an OpenSSL
                    verify constant ('none' or 'peer')
                data[config][mailer][smtp_settings][password]:
                  type: string
                  description: SMTP password
                data[config][mailer][smtp_settings][port]:
                  type: number
                  description: Port of SMTP server
                data[config][mailer][smtp_settings][read_timeout]:
                  type: number
                  description: Number of seconds to wait until timing-out a read(2)
                    call.
                data[config][mailer][smtp_settings][ssl]:
                  type: string
                  description: 'Enables the SMTP connection to use SMTP/TLS (SMTPS:
                    SMTP over direct TLS connection)'
                data[config][mailer][smtp_settings][user_name]:
                  type: string
                  description: SMTP username
                data[config][mailer][support_email]:
                  type: string
                  description: The support address used in mails for user email confirmations
                data[config][theme][background][advanced]:
                  type: string
                  description: background CSS property
                  nullable: true
                data[config][theme][background][default]:
                  type: string
                  description: background color
                data[config][theme][components_care][ui_kit][action_button][background_color]:
                  type: string
                  description: background color
                data[config][theme][primary][dark]:
                  type: string
                  description: darker variant of the primary color
                data[config][theme][primary][light]:
                  type: string
                  description: lighter variant of the primary color
                data[config][theme][primary][main]:
                  type: string
                  description: primary color
                data[config][theme][secondary][dark]:
                  type: string
                  description: darker variant of the secondary color
                data[config][theme][secondary][light]:
                  type: string
                  description: lighter variant of the secondary color
                data[config][theme][secondary][main]:
                  type: string
                  description: secondary color
                data[config][theme][type]:
                  type: string
                  default: light
                  description: light or dark theme as base
                data[config][url]:
                  type: string
                  default: https://domain.local
                  description: URL of the app
                data[config][uses_bearer_token]:
                  type: boolean
                  default: true
                  description: 'If the app bearer token will be included in redirect
                    URL as #anchor (true) or query param for server (false)'
                data[full_name]:
                  type: string
                  default: app-name
                  description: full name of the app
                data[import_candos]:
                  type: string
                  description: yaml data with array of all candos the app has
                data[import_roles]:
                  type: string
                  description: yaml data with array of all roles the app has and assigned
                    candos
                data[locale_import_candos][de]:
                  type: string
                  description: the german locales for this apps functionalities as
                    yaml string
                data[locale_import_candos][en]:
                  type: string
                  description: the english locales for this apps functionalities as
                    yaml string
                data[locale_import_roles][de]:
                  type: string
                  description: the german locales for this apps roles as yaml string
                data[locale_import_roles][en]:
                  type: string
                  description: the english locales for this apps roles as yaml string
                data[short_name]:
                  type: string
                  default: app-name
                  description: short name of the app

