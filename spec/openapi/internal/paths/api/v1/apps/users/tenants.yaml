# Version: 1.0
# auto generated path descriptions, update via: (append `-f` to force update if needed)
#     rails g openapi_spec v1_app_user_tenants
---
paths:
  "/api/v1/apps/{app_id}/users/{user_id}/tenants":
    get:
      summary: 'Tenant: List [GET] a User has in an App'
      tags:
      - User Tenants
      operationId: apps-users-tenants-index
      x-controller: Api::V1::Apps::Users::TenantsController
      x-controller-action: index
      x-route-name: v1_app_user_tenants
      x-candos:
      - "~/apps.admin+identity-management/actors.reader"
      x-model-type: Actors::Tenant
      x-record-type: tenant
      x-gridfilter-fields:
      - _id
      - _keywords
      - _type
      - active
      - actor_settings
      - auto
      - children_count
      - created_at
      - defaults_checksum
      - deleted
      - deleted_at
      - depth
      - full_name
      - image_data
      - map_actor_id
      - modules_selected
      - name
      - parent_id
      - parent_ids
      - path
      - path_ids
      - role_ids
      - short_name
      - system
      - template_actor_id
      - title
      - updated_at
      - write_protected
      description: |-
        Tenants an app user belongs to

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - ~/apps.admin+identity-management/actors.reader
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: app_id
        in: path
        required: true
        description: Tenant app_id
        schema:
          type: string
      - name: user_id
        in: path
        required: true
        description: Tenant user_id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      - name: quickfilter
        in: query
        schema:
          type: string
        description: performs a search in keywords
      - name: page[number]
        in: query
        schema:
          type: string
        x-example: '1'
        description: |
          the page number (starts at 1, if not given or lower than 1 then
          page 1 will automatically be assumed)'
      - name: page[limit]
        in: query
        schema:
          type: string
        x-example: '10'
        description: the number of records per page (might be limited on the server
          side, use 0 to fetch no data to get only the total count)
      - name: padding
        in: query
        schema:
          type: string
        x-example: '0'
        description: 'manually adjust db offset: (page*per_page)+padding not required
          for normal paging, in doubt omit'
      - name: sort
        in: query
        schema:
          type: string
        description: |
          JSON Array of Objects to sort the results by one
          or more fields in the requested order.
          Example:
          `[{ property: "field1", direction: "ASC" }, { property: "field2", direction: "DESC" }]`
      - name: gridfilter
        in: query
        schema:
          type: string
        description: |
          String in JSON format for grid-based filtering.

          <details>
            <summary>The model defined these fields as allowed for filtering.</summary>

            `_id`, `_keywords`, `_type`, `active`, `actor_settings`, `auto`, `children_count`, `created_at`, `defaults_checksum`, `deleted`, `deleted_at`, `depth`, `full_name`, `image_data`, `map_actor_id`, `modules_selected`, `name`, `parent_id`, `parent_ids`, `path`, `path_ids`, `role_ids`, `short_name`, `system`, `template_actor_id`, `title`, `updated_at`, `write_protected`

          </details>
      responses:
        '200':
          description: Actors::Tenants list
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: List endpoints will return an array below data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - type
                      - attributes
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          default: tenant
                        attributes:
                          "$ref": "#/components/schemas/TenantSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
  "/api/v1/apps/{app_id}/users/{user_id}/tenants/{id}":
    get:
      summary: 'Tenant: Show [GET] a User has in an App'
      tags:
      - User Tenants
      operationId: apps-users-tenants-show
      x-controller: Api::V1::Apps::Users::TenantsController
      x-controller-action: show
      x-route-name: v1_app_user_tenants
      x-candos:
      - "~/apps.admin+identity-management/actors.reader"
      x-model-type: Actors::Tenant
      x-record-type: tenant
      description: |-
        Tenants an app user belongs to

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - ~/apps.admin+identity-management/actors.reader
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: query
        required: true
        schema:
          type: string
        description: |
          The tenant context is required to determine if the user has
          the required privileges.
      - name: app_id
        in: path
        required: true
        description: Tenant app_id
        schema:
          type: string
      - name: user_id
        in: path
        required: true
        description: Tenant user_id
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Tenant id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::Tenant view
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: tenant
                      attributes:
                        "$ref": "#/components/schemas/TenantSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"

