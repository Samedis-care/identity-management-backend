# Version: 1.0
# auto generated path descriptions, update via: (append `-f` to force update if needed)
#     rails g openapi_spec v1_picker_tenant_users
---
paths:
  "/api/v1/picker/tenant/{tenant_id}/users":
    get:
      summary: 'Tenant User: List [GET]'
      tags:
      - 'Picker: Tenant Users'
      operationId: picker-tenantusers-index
      x-controller: Api::V1::Picker::TenantUsersController
      x-controller-action: index
      x-route-name: v1_picker_tenant_users
      x-candos:
      - "~/list-tenant-users.reader"
      x-model-type: User
      x-record-type: tenant_user
      x-gridfilter-fields:
      - actor_id
      - created_at
      - email
      - first_name
      - gender
      - id
      - last_name
      - locale
      - title
      description: |-
        Tenant user picker for selection dialogs

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - ~/list-tenant-users.reader
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: path
        required: true
        description: Tenant User tenant_id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      - name: quickfilter
        in: query
        schema:
          type: string
        description: performs a search in keywords
      - name: page[number]
        in: query
        schema:
          type: string
        x-example: '1'
        description: |
          the page number (starts at 1, if not given or lower than 1 then
          page 1 will automatically be assumed)'
      - name: page[limit]
        in: query
        schema:
          type: string
        x-example: '10'
        description: the number of records per page (might be limited on the server
          side, use 0 to fetch no data to get only the total count)
      - name: padding
        in: query
        schema:
          type: string
        x-example: '0'
        description: 'manually adjust db offset: (page*per_page)+padding not required
          for normal paging, in doubt omit'
      - name: sort
        in: query
        schema:
          type: string
        description: |
          JSON Array of Objects to sort the results by one
          or more fields in the requested order.
          Example:
          `[{ property: "field1", direction: "ASC" }, { property: "field2", direction: "DESC" }]`
      - name: gridfilter
        in: query
        schema:
          type: string
        description: |
          String in JSON format for grid-based filtering.

          <details>
            <summary>The model defined these fields as allowed for filtering.</summary>

            `actor_id`, `created_at`, `email`, `first_name`, `gender`, `id`, `last_name`, `locale`, `title`

          </details>
      responses:
        '200':
          description: Users list
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: List endpoints will return an array below data
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required:
                      - id
                      - type
                      - attributes
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          default: tenant_user_overview
                        attributes:
                          "$ref": "#/components/schemas/TenantUserOverviewSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
  "/api/v1/picker/tenant/{tenant_id}/users/{id}":
    get:
      summary: 'Tenant User: Show [GET]'
      tags:
      - 'Picker: Tenant Users'
      operationId: picker-tenantusers-show
      x-controller: Api::V1::Picker::TenantUsersController
      x-controller-action: show
      x-route-name: v1_picker_tenant_users
      x-candos:
      - "~/list-tenant-users.reader"
      x-model-type: User
      x-record-type: tenant_user
      description: |-
        Tenant user picker for selection dialogs

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - ~/list-tenant-users.reader
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: path
        required: true
        description: Tenant User tenant_id
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Tenant User id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: User view
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: tenant_user
                      attributes:
                        "$ref": "#/components/schemas/TenantUserSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
    put:
      summary: 'Tenant User: Update [PUT]'
      tags:
      - 'Picker: Tenant Users'
      operationId: picker-tenantusers-update
      x-controller: Api::V1::Picker::TenantUsersController
      x-controller-action: update
      x-route-name: v1_picker_tenant_users
      x-candos:
      - "~/list-tenant-users.writer"
      x-model-type: User
      x-record-type: tenant_user
      description: |-
        Tenant user picker for selection dialogs

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - ~/list-tenant-users.writer
      security:
      - Bearer: []
      parameters:
      - name: tenant_id
        in: path
        required: true
        description: Tenant User tenant_id
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Tenant User id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: tenant_user
                      attributes:
                        "$ref": "#/components/schemas/TenantUserSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true
          application/x-www-form-urlencoded:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true
          multipart/form-data:
            schema:
              type: object
              description: This endpoint does not support sending any attributes
              nullable: true

