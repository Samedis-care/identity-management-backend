# Version: 1.0
# auto generated path descriptions, update via: (append `-f` to force update if needed)
#     rails g openapi_spec v1_invitations
---
paths:
  "/api/v1/{app}/{tenant_id}/invitations":
    post:
      summary: 'Invitation: Create [POST]'
      tags:
      - Tenant Invitations
      operationId: app-tenant-invitations-create
      x-controller: Api::V1::App::Tenant::InvitationsController
      x-controller-action: create
      x-route-name: v1_invitations
      x-candos:
      - "~/invitations.writer"
      - "~/access-control.writer"
      - "~/tenant.admin"
      - "~/app-tenant.admin"
      x-model-type: Invite
      x-record-type: invitation
      description: |-
        Manage tenant invitations for an app

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - ~/invitations.writer
         - ~/access-control.writer
         - ~/tenant.admin
         - ~/app-tenant.admin
      security:
      - Bearer: []
      parameters:
      - name: app
        in: path
        required: true
        description: Invitation app
        schema:
          type: string
      - name: tenant_id
        in: path
        required: true
        description: Invitation tenant_id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Invite created
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: invitation
                      attributes:
                        "$ref": "#/components/schemas/InvitationSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: object
                  description: the main attributes of this record
                  properties:
                    actions:
                      type: object
                      description: JSON Hash with key value pairs of one or more actions
                        (key) to run on accept with arguments (value)
                      properties:
                        access_group_ids:
                          type: array
                          description: ids to the groups below a tenant organization
                            to authorize the user acception invites (removes all previous)
                        access_groups:
                          type: array
                          description: name identifiers in the form of `ou-name/group-name`
                            to the groups below a tenant organization to authorize
                            the user acception invites (removes all previous)
                        add_access_group_ids:
                          type: array
                          description: ids to the groups below a tenant organization
                            to authorize the user acception invites (adds to those
                            the user already has)
                        add_access_groups:
                          type: array
                          description: name identifiers in the form of `ou-name/group-name`
                            to the groups below a tenant organization to authorize
                            the user acception invites (adds to those the user already
                            has)
                    auto_accept:
                      type: boolean
                      default: true
                      description: automatically performs the invitation as soon as
                        the user is logging in
                    email:
                      type: string
                      writeOnly: true
                      description: e-mail of the user to invite
                    invitable_id:
                      type: string
                      description: the id of the object this invitation refers to
                    invitable_type:
                      type: string
                      default: tenant
                      description: the object class this invitation refers to
                    target_url:
                      type: string
                      description: optional URL the invitee is redirected to after
                        accepting; also used to derive redirect_url
                    user_id:
                      type: string
                      writeOnly: true
                      description: Optionally the user id instead of email user to
                        invite
                    valid_until:
                      type: string
                      format: date-time
                      description: the effective expiry of this invitation (server-clamped;
                        see Invite::MAX_VALID_UNTIL) - accepting it after this timestamp
                        no longer works
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                data[actions][access_group_ids]:
                  type: array
                  description: ids to the groups below a tenant organization to authorize
                    the user acception invites (removes all previous)
                data[actions][access_groups]:
                  type: array
                  description: name identifiers in the form of `ou-name/group-name`
                    to the groups below a tenant organization to authorize the user
                    acception invites (removes all previous)
                data[actions][add_access_group_ids]:
                  type: array
                  description: ids to the groups below a tenant organization to authorize
                    the user acception invites (adds to those the user already has)
                data[actions][add_access_groups]:
                  type: array
                  description: name identifiers in the form of `ou-name/group-name`
                    to the groups below a tenant organization to authorize the user
                    acception invites (adds to those the user already has)
                data[auto_accept]:
                  type: boolean
                  default: true
                  description: automatically performs the invitation as soon as the
                    user is logging in
                data[email]:
                  type: string
                  writeOnly: true
                  description: e-mail of the user to invite
                data[invitable_id]:
                  type: string
                  description: the id of the object this invitation refers to
                data[invitable_type]:
                  type: string
                  default: tenant
                  description: the object class this invitation refers to
                data[target_url]:
                  type: string
                  description: optional URL the invitee is redirected to after accepting;
                    also used to derive redirect_url
                data[user_id]:
                  type: string
                  writeOnly: true
                  description: Optionally the user id instead of email user to invite
                data[valid_until]:
                  type: string
                  format: date-time
                  description: the effective expiry of this invitation (server-clamped;
                    see Invite::MAX_VALID_UNTIL) - accepting it after this timestamp
                    no longer works
          multipart/form-data:
            schema:
              type: object
              properties:
                data[actions][access_group_ids]:
                  type: array
                  description: ids to the groups below a tenant organization to authorize
                    the user acception invites (removes all previous)
                data[actions][access_groups]:
                  type: array
                  description: name identifiers in the form of `ou-name/group-name`
                    to the groups below a tenant organization to authorize the user
                    acception invites (removes all previous)
                data[actions][add_access_group_ids]:
                  type: array
                  description: ids to the groups below a tenant organization to authorize
                    the user acception invites (adds to those the user already has)
                data[actions][add_access_groups]:
                  type: array
                  description: name identifiers in the form of `ou-name/group-name`
                    to the groups below a tenant organization to authorize the user
                    acception invites (adds to those the user already has)
                data[auto_accept]:
                  type: boolean
                  default: true
                  description: automatically performs the invitation as soon as the
                    user is logging in
                data[email]:
                  type: string
                  writeOnly: true
                  description: e-mail of the user to invite
                data[invitable_id]:
                  type: string
                  description: the id of the object this invitation refers to
                data[invitable_type]:
                  type: string
                  default: tenant
                  description: the object class this invitation refers to
                data[target_url]:
                  type: string
                  description: optional URL the invitee is redirected to after accepting;
                    also used to derive redirect_url
                data[user_id]:
                  type: string
                  writeOnly: true
                  description: Optionally the user id instead of email user to invite
                data[valid_until]:
                  type: string
                  format: date-time
                  description: the effective expiry of this invitation (server-clamped;
                    see Invite::MAX_VALID_UNTIL) - accepting it after this timestamp
                    no longer works
  "/api/v1/{app}/{tenant_id}/invitations/{id}":
    delete:
      summary: 'Invitation: Delete [DELETE]'
      tags:
      - Tenant Invitations
      operationId: app-tenant-invitations-destroy
      x-controller: Api::V1::App::Tenant::InvitationsController
      x-controller-action: destroy
      x-route-name: v1_invitations
      x-candos:
      - "~/invitations.writer"
      - "~/tenant.admin"
      - "~/app-tenant.admin"
      x-model-type: Invite
      x-record-type: invitation
      description: |-
        Manage tenant invitations for an app

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - ~/invitations.writer
         - ~/tenant.admin
         - ~/app-tenant.admin
      security:
      - Bearer: []
      parameters:
      - name: app
        in: path
        required: true
        description: Invitation app
        schema:
          type: string
      - name: tenant_id
        in: path
        required: true
        description: Invitation tenant_id
        schema:
          type: string
      - name: id
        in: path
        required: true
        description: Invitation id
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Invite deleted
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/meta"
        '409':
          description: Conflict
          content:
            application/json:
              schema:
                type: object
                description: |-
                  Some server side rule prevented finishing this request.
                  For example a multi delete might not have worked on all
                  requested records due to some rule not allowing the delete.
                  Please refer to the details in the response like
                  - `meta.msg.error` error identifier
                  - `meta.msg.message` message being displayed
                  - `meta.msg.error_details` showing the attribute causing the problem
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"

