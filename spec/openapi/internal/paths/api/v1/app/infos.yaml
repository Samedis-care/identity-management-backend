# Version: 1.0
# auto generated path descriptions, update via: (append `-f` to force update if needed)
#     rails g openapi_spec v1_app_info
---
paths:
  "/api/v1/app/info/{name}":
    get:
      summary: 'App Info: Show [GET]'
      tags:
      - App Info
      operationId: app-infos-show
      x-controller: Api::V1::App::InfosController
      x-controller-action: show
      x-route-name: v1_app_info
      x-candos:
      - public
      x-model-type: Actors::App
      x-record-type: app_info
      description: |-
        Public app information and branding

        ---

        To use this endpoint the current user needs to be authorized for the
        tenant to do any of these

         - public
      security:
      - Bearer: []
      parameters:
      - name: name
        in: path
        required: true
        description: App Info name
        schema:
          type: string
      - name: locale
        in: query
        description: to control the language returned
        schema:
          "$ref": "#/components/schemas/locales"
      responses:
        '200':
          description: Actors::App view
          content:
            application/json:
              schema:
                type: object
                required:
                - data
                description: Single endpoints will return an object as data
                properties:
                  data:
                    type: object
                    required:
                    - id
                    - type
                    - attributes
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                        default: app_info
                      attributes:
                        "$ref": "#/components/schemas/AppInfoSerializer"
                  meta:
                    "$ref": "#/components/schemas/meta"
        '400':
          description: Some values that were sent couldn't be processed
          content:
            application/json:
              schema:
                type: object
                description: Check the returned info in meta/msg for details
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '429':
          description: Too many requests
          content:
            application/json:
              schema:
                type: object
                description: Wait for so many seconds as returned in the Retry-After
                  header before accessing this endpoint again
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema:
                type: object
                description: Requires a valid login
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"
        '403':
          description: Unauthorized access
          content:
            application/json:
              schema:
                type: object
                description: User is missing the access rights to do this
                properties:
                  meta:
                    "$ref": "#/components/schemas/meta"

